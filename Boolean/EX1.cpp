#include<bits/stdc++.h>
using namespace std;
int main(){
    
    int myAge = 25;
    int votingAge = 18;
    
    if (myAge >= votingAge)
    {
     cout << "old enough to vote!";
    } else  {
     cout << "Not enough to vote.";
    }
    return 0;
}